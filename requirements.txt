# Dependencies are defined in pyproject.toml — install with:
#     pip install .
#
# This file is kept for backwards compatibility with `pipeline.sh` and
# tools that still expect a requirements.txt.

# GUI
PySide6>=6.6

# Face detection / alignment
mediapipe>=0.10.0
dlib-bin>=20.0.1
opencv-python>=4.8.0

# Background removal (heavy ML stack)
torch>=2.5
torchvision>=0.20
transformers>=4.45
timm>=1.0
einops>=0.8
huggingface_hub>=0.25
kornia>=0.7

# Image processing
Pillow>=10.0.0
numpy>=1.26
scipy>=1.11

# Download queue
requests>=2.31
beautifulsoup4>=4.12
